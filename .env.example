# ===========================================
# EMAIL OTP CONFIGURATION
# ===========================================
# Email service configuration for OTP delivery

# OTP Secret for hashing (change in production!)
OTP_SECRET=your-secret-key-for-otp-hashing-change-in-production

# Email Service Provider Options:
# Option 1: Gmail (requires App Password)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Option 2: SendGrid
# SENDGRID_API_KEY=your-sendgrid-api-key

# Option 3: Custom SMTP
# SMTP_HOST=your-smtp-server.com
# SMTP_PORT=587
# SMTP_USER=your-username
# SMTP_PASS=your-password

# ===========================================
# FIREBASE CONFIGURATION
# ===========================================
# These are already configured in your Firebase app
# No additional environment variables needed for Firebase

# ===========================================
# SETUP INSTRUCTIONS - EMAIL OTP
# ===========================================
# 1. Choose an email service provider:
#    - Gmail: Enable 2FA and create App Password
#    - SendGrid: Sign up and get API key
#    - Custom SMTP: Get credentials from your provider
# 
# 2. Set up Firebase Cloud Functions (recommended):
#    - Deploy the provided Cloud Function
#    - Configure email credentials via Firebase CLI:
#      firebase functions:config:set email.user="your-email"
#      firebase functions:config:set email.password="your-password"
#
# 3. Alternative: Set SMTP credentials in .env for direct sending
#
# 4. Update OTP_SECRET to a strong random string in production
#
# 5. Test the email flow with a real email address

# ===========================================
# PRODUCTION SECURITY NOTES
# ===========================================
# - Never commit .env files to version control
# - Use Firebase Functions config for email credentials
# - Rotate OTP_SECRET regularly
# - Monitor email delivery rates and costs
# - Implement proper logging for debugging